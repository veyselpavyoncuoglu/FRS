# ── Build outputs ──────────────────────────────────────────────────────────────
FRS.exe
FRS

# Intermediate / temp build artefacts
build/
*.o
*.d
*.a
*.lib

# Compiled Windows resource (generated by windres from res/app.rc)
res/app.res

# ── Downloaded dependencies ────────────────────────────────────────────────────
# ImGui source files — fetched and copied by setup.bat / setup.sh.
# Commit the setup scripts, not the downloaded library.
imgui/

# Local MinGW-w64 toolchain — downloaded by setup.bat when g++ is not in PATH.
# Only the helper script belongs in the repo, not the hundreds of MB of binaries.
tools/mingw64/

# ── OS noise ───────────────────────────────────────────────────────────────────
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
desktop.ini
$RECYCLE.BIN/

# ── Editor / IDE ───────────────────────────────────────────────────────────────
# Visual Studio
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.VC.db
*.VC.opendb

# VS Code
.vscode/

# JetBrains / CLion
.idea/
cmake-build-*/

# ── Misc ───────────────────────────────────────────────────────────────────────
*.log
*.tmp
*.bak
